import setuptools
import numpy.distutils.core

if __name__ == "__main__":
    numpy.distutils.core.setup(
        name='fortress',
        version='0.1.5',
        platforms='linux',
        packages=['fortress'])
